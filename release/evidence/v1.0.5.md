# clickhousex v1.0.5 Evidence

Date: 2026-06-19
Scope: complete ClickHouse client coverage gate alignment.

Verified locally:

- `go test ./... -covermode=atomic -coverprofile=coverage.out`
- `go tool cover -func=coverage.out` reports `total: (statements) 100.0%`
- `go test ./...`
- `go test -race ./...`
- `go vet ./...`
- `go build ./...`
- `go list -deps ./... | rg '(^|/)configx($|/)'` returns no matches
- `xlibgate trust identity --repo .`
- `xlibgate trust template-residue --repo .`
- `xlibgate trust secret-redaction --repo . --path release/evidence`
- `git diff --check`

Release consistency inputs updated:

- `VERSION`
- `.repo-contract.yaml`
- `CHANGELOG.md`
- `release/manifest/latest.json`
- `pkg/clickhousex/version.go`

Notes:

- v1.0.5 keeps the v1.0.4 client runtime surface unchanged and tightens release evidence to satisfy the exact 100.0% coverage gate.
- Additional tests cover previously unproven branches in default validation, query setup cancellation, close wait cancellation, retry waits, retry-delay clamping, and native option adapter setup.
- External ClickHouse integration tests were not run locally; behavior remains covered with fake-driver unit tests and native adapter compilation.
