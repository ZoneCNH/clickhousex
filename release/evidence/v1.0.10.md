# clickhousex v1.0.10 Release Evidence

Date: 2026-06-21

## Scope

This release aligns the repository metadata and release markers to v1.0.10 while keeping the existing L2-T3 release posture:

- CI/CD workflows remain configured for release readiness and factory evidence collection.
- `make release-check` remains the production release gate.
- The current release level stays at L2-T3; factory-grade blockers are unchanged.

## Evidence

| Check | Command | Result |
| --- | --- | --- |
| Metadata alignment | `cat VERSION; jq -r .version release/manifest/latest.json; jq -r .release_version .agent/evidence/decision/release-readiness.json` | PASS |
| Release gate | `make release-check` | PASS |
| Typecheck / compile | `go test ./...` | PASS |
| Lint | `make lint` | PASS |
| Patch hygiene | `git diff --check` | PASS |

## Release Decision

- `release_level_actual`: `L2-T3`
- `release_allowed`: `true`
- `factory_grade`: `false`

`make factory-check` remains blocked until multi-hour soak, external rollout, and factory archive evidence are archived.
