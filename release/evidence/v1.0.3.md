# clickhousex v1.0.3 Evidence

Date: 2026-06-19
Scope: release gate metadata and CI trust alignment.

Verified locally:

- `go test ./...`
- `go test ./... -race -count=1`
- `go vet ./...`
- `go build ./...`
- `golangci-lint run ./...`
- `go test ./... -coverprofile=coverage.out`
- `xlibgate trust identity --repo .`
- `xlibgate trust template-residue --repo .`
- `xlibgate trust secret-redaction --repo . --path release/evidence`
- Local release metadata consistency check over `VERSION`, `.repo-contract.yaml`, `CHANGELOG.md`, `release/manifest/latest.json`, and `pkg/clickhousex/version.go`.

Notes:

- v1.0.2 remains published history.
- v1.0.3 pins xlibgate to a commit containing the trust CLI group and adds release consistency inputs.
- CI keeps release metadata consistency local until xlibgate publishes a fixed `release-consistency` command that excludes the module path from version comparison.
