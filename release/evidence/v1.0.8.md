# clickhousex v1.0.8 Evidence

Date: 2026-06-19
Scope: re-verification release — re-run the complete client release gate against the local dev ClickHouse instance and re-align version metadata.

Verified locally:

- `GOWORK=off go test ./...`
- `GOWORK=off go test -race ./...`
- `GOWORK=off go vet ./...`
- `GOWORK=off go build ./...`
- `GOWORK=off golangci-lint run ./...`
- `GOWORK=off go test ./... -covermode=atomic -coverprofile=coverage.out`
- `GOWORK=off go tool cover -func=coverage.out`
- `CLICKHOUSEX_RUN_INTEGRATION=1 GOWORK=off go test -count=1 -run TestClickHouseLiveIntegration -v ./pkg/clickhousex`
- `CLICKHOUSEX_RUN_INTEGRATION=1 CLICKHOUSEX_RUN_SOAK=1 CLICKHOUSEX_SOAK_DURATION=60s CLICKHOUSEX_SOAK_INTERVAL=100ms GOWORK=off go test -count=1 -run TestClickHouseLiveSoak -v ./pkg/clickhousex`
- `GOWORK=off go test -run '^$' -bench . -benchmem -cpuprofile /tmp/clickhousex-v1.0.8.cpu.pprof -memprofile /tmp/clickhousex-v1.0.8.mem.pprof ./pkg/clickhousex`
- `GOWORK=off go list -deps ./...` with no `configx` dependency
- `git diff --check`

Results:

- Default unit/integration-skipped suite: pass (`internal/sanitize`, `internal/validation`, `pkg/clickhousex`).
- Race suite: pass.
- Vet/build/lint: pass (exit 0).
- Coverage: `total: (statements) 100.0%` across all three packages; every function at 100.0%.
- Live integration: pass against the dev ClickHouse endpoint (26.5.2.39) projected from `sre/secrets/env/dev.md` into environment variables; no credential values are recorded here.
- Live soak: pass, `duration=1m0s`, `iterations=366`, `interval=100ms`.
- Dependency gate: no `configx` dependency.
- Patch format: pass.

Benchmark and profile evidence:

```text
goos: linux
goarch: amd64
pkg: github.com/ZoneCNH/clickhousex/pkg/clickhousex
cpu: Intel(R) Xeon(R) W-10885M CPU @ 2.40GHz
BenchmarkClientExec-16             	  727092	      1474 ns/op	     848 B/op	       8 allocs/op
BenchmarkClientQueryRowsScan-16    	  835095	      1672 ns/op	     984 B/op	       15 allocs/op
BenchmarkClientInsertBatch-16      	  622778	      2013 ns/op	     800 B/op	       12 allocs/op
BenchmarkClientHealthCheck-16      	 2717629	       485.4 ns/op	     336 B/op	       2 allocs/op
```

Generated local profile artifacts:

- `/tmp/clickhousex-v1.0.8.cpu.pprof`
- `/tmp/clickhousex-v1.0.8.mem.pprof`

Version metadata re-aligned to v1.0.8:

- `VERSION` → `v1.0.8`
- `.repo-contract.yaml` `versions.table_version` and `versions.latest_git_tag` → `v1.0.8`
- `pkg/clickhousex/version.go` `Version` → `v1.0.8`
- `release/manifest/latest.json` `version` → `v1.0.8`
- `CHANGELOG.md` adds the `## v1.0.8` entry.

Merge:

- The `clickhousex` integration branch (carrying the v1.0.3–v1.0.8 release line) is merged into `main` so the default branch reaches v1.0.8.

Remaining known gaps (unchanged from v1.0.7):

- Production-duration multi-hour soak.
- External consumer adoption rollout.
