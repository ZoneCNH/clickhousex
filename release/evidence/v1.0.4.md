# clickhousex v1.0.4 Evidence

Date: 2026-06-19
Scope: complete ClickHouse client runtime surface.

Verified locally:

- `go test ./...`
- `go test -race ./...`
- `go vet ./...`
- `go build ./...`
- `git diff --check`
- `go list -m -json github.com/ClickHouse/ch-go` confirms `v0.67.0` with `GoVersion: 1.23.0`.

Release consistency inputs updated:

- `VERSION`
- `.repo-contract.yaml`
- `CHANGELOG.md`
- `release/manifest/latest.json`
- `pkg/clickhousex/version.go`

Notes:

- v1.0.4 replaces the previous metadata-only client state with a real `clickhouse-go/v2` backed client.
- The module remains on Go 1.23 by pinning the indirect `ch-go` dependency to the version declared by `clickhouse-go/v2 v2.39.0`.
- External ClickHouse integration tests were not run locally; behavior is covered with fake-driver unit tests and native adapter compilation.
